<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khieuthichien.hotrohoctap">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".news.NewsActivity"
            android:label="News" />
        <activity
            android:name=".news.NewsDetailActivity"
            android:label="News" />
        <activity
            android:name=".news.TintucActivity"
            android:label="News" />
        <activity
            android:name=".CoursesActivity"
            android:label="@string/title_activity_courses"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DangkikhoahocActivity"
            android:label="@string/title_activity_dangkikhoahoc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".KhoahocActivity" />
        <activity android:name=".MonhocActivity" />
        <activity android:name=".LichhocActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".LichthiActivity"></activity>
    </application>

</manifest>